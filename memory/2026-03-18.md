# 2026-03-18

## Today

- Confirmed the project-level `memory` directory is present and remains the daily progress ledger.
- Created today's dated progress note so development updates can continue to be appended throughout the day.
- Carried forward the current acceptance focus from the prior session: login stability, expert-review workflow, and deployed Vercel verification.
- Updated filename routing so evidence names like `2.8.1.1默认账号...png` and `2.8.2.1关闭非必要公网访问端口...png` now auto-match checklist codes even when Chinese descriptions follow the code directly.
- Re-verified that global report names like `安扫报告.pdf` still route as shared evidence, and confirmed `npm run build` passes after the routing change.
- Reviewed the Vercel database SSL warning, confirmed it was non-blocking, and tightened the PostgreSQL client defaults to normalize `sslmode=verify-full` unless an explicit insecure override is requested.
- Updated `.env.example` to recommend `PGSSLMODE=verify-full`, and re-verified both build output and a live PostgreSQL connectivity check under the new TLS settings.
- Investigated Vercel `300s` runtime timeouts on `/api/analyze`, identified the main bottleneck as sequential OCR plus per-image vision plus mandatory-item rechecks, and changed the analyzer to use bounded concurrency.
- Added graceful frontend handling for non-JSON timeout responses so users now see a clear “analysis timed out, split the batch” message instead of a JSON parse error.
- Added deployment knobs for large batches: `DASHSCOPE_INLINE_VISION_MAX_FILES`, `DASHSCOPE_MANDATORY_VISION_CONCURRENCY`, and `ANALYZE_FILE_CONCURRENCY`.
- Added a concise upload guidance line in the intake area so the UI now explicitly shows file-count, file-size, ZIP restriction, naming examples, and the “critical evidence should be uploaded as standalone images” recommendation.
- Added a frontend batch-size reminder: when selected files exceed the recommended range, the intake area now shows a split-batch suggestion based on total files, image count, and distinct checklist codes inferred from filenames.
- Promoted `业务名称` to a first-class required field before analysis: the frontend now requires it before upload/analysis, and the backend accepts `businessName` explicitly while remaining compatible with the older `caseName` field.
- Carried the business name through persistence, case history, and review export so subsequent AI analysis, expert review, and result lookup all stay anchored to the same business identifier.
- Optimized the historical case archive for acceptance testing: expanded case-list retrieval to the most recent 200 records, added keyword search plus workflow filtering, surfaced hit-count statistics, and grouped matching cases into “最近 7 天” and “更早案件” sections for faster review lookup.
- Tightened the intake-area copy into short operational rules so the UI now states the file-count limit, single-file size limit, ZIP restriction, recommended filename format, and “standalone images first” guidance in a scannable format.
- Reworked the active-review experience for acceptance feedback: the checklist now defaults to “本次命中” instead of showing the entire full list after every upload, supports inline search by code/requirement/category, highlights “已命中材料” at the row level, and shows scoped counts for hit items, pending items, and the current view.
- Reduced page fatigue by collapsing the full review-flow explanation behind a “查看流程详情” toggle and replacing the old static category distribution block with actionable current-batch metrics.
- Refined the archive/workbench split after further acceptance feedback: historical cases are now organized as “审核中项目” and “已审核项目”, while the main result pane shifts from a full-checklist mindset to a result-first view that defaults to problem items, then matched items, with the complete checklist retained as an explicit appendix view.
- Simplified upload guidance to match real acceptance behavior: removed the explicit “20 files” wording from the UI, normalized the recommendation to “5–8 files per batch”, and aligned the timeout hint plus batch-warning threshold with that same split-upload guidance.
- Added a generic security-scan-report assessment path: reports named like `安扫报告/扫描报告/漏洞扫描` now produce a dedicated scan conclusion that checks for device inventory, per-device vulnerability detail, and unresolved medium/high-risk findings without binding to any single vendor template; the result is surfaced both in checklist reasoning and as a standalone “Security Scan” card.
- Ran a lightweight synthetic validation for the new scan-report parser using a text-only mock report; confirmed the chain returns structured JSON with extracted device count, per-device risk details, and a conservative fail/insufficient-evidence outcome when medium/high-risk handling is unclear.
- Improved login usability for acceptance and handoff: the password field on the login screen now includes an inline show/hide toggle so operators and experts can verify credentials without retyping.
- Cleaned up the archive list for real-world use: known demo/acceptance case names such as `语音业务接入审核案件` and `工作流自动送审验收` are now hidden by default in the history panel, with an explicit toggle to reveal them when needed.

## Next

- Continue recording substantive code, deployment, and verification progress in this file as work advances today.
