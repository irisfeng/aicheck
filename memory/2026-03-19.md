# 2026-03-19

## Today

- Cleared the historical acceptance dataset before the next manual test round: deleted the remaining `1` case from `review_cases`, removed its linked `21` Cloudflare R2 objects, and re-verified the database returned to `0` cases.
- Fixed the post-expert supplementation rule so passed items stay locked after a case enters `pending_expert_review` / `expert_reviewed`; later uploads now only re-review previously unresolved items hit by the new batch, keep unchanged pass items from the last accepted state, and preserve workflow state when a supplement batch does not actually trigger any re-review.
- Added incremental review scope metadata to `/api/analyze` responses so the case payload now records whether the run was `full_review` or `incremental_unresolved`, plus which codes were re-reviewed and which pass codes were preserved.
- Rebuilt the frontend bundle with the new backend logic to confirm the repository still compiles cleanly via `npm run build`.

## Next

- Continue recording substantive code, deployment, verification, and data-reset steps in this file as work advances today.
