# 2026-03-17

## Progress

- Confirmed the MVP direction was switched to full API mode.
- Chose a domestic-only API path with DashScope / Bailian as the default provider.
- Created the project skeleton for a frontend review workbench plus a backend orchestration API.
- Added a daily `memory` directory workflow as requested.
- Implemented the frontend MVP shell with upload, checklist view, detail panel, manual overrides, and export.
- Implemented the backend API flow for file intake, OCR routing, document text extraction, and checklist review orchestration.
- Installed dependencies and verified `vite build` passes.
- Verified the server starts and `/api/health` returns the expected provider status.
- Added filename-based evidence routing so files like `2.8.1.1-1.png` automatically map to the matching checklist item.
- Added global-evidence recognition for files like `安扫报告.pdf`.
- Renamed the product UI to `语音业务对接入网技术审核台`.
- Added a minimal login module with two roles: operator and expert.
- Restricted manual override and export to expert accounts for the MVP.
- Connected mandatory-item visual recheck so required items with matching screenshots now go through OCR + vision dual judgment.
- Re-verified the updated pipeline with a production build and a clean server smoke test on port `8791`.
- Added a homepage "审查工作机制" section so operators and experts can see the intake-to-review workflow at a glance.
- Replaced in-memory login sessions with JWT so the app is compatible with Vercel-style stateless deployment.
- Added PostgreSQL-ready case persistence with an in-memory fallback for local development.
- Split the backend into a reusable API router plus a Vercel entry, and added minimal case-history loading in the UI.
- Added Cloudflare R2 direct-upload signing plus backend object fetch, so raw files can be stored outside Vercel while keeping the current review flow.
- Added handoff documentation for database auto-init, R2 runtime integration, and local-vs-Vercel behavior differences.

## Current Build Scope

- Frontend: internal review workbench
- Backend: file intake, OCR routing, checklist review orchestration
- Provider default: `qwen-vl-ocr-latest` + `qwen-flash`

## Next

- Wire a real DashScope key into `.env`
- Run an end-to-end analysis with real case materials
- Tighten prompts based on the first internal review results
- Evaluate whether ZIP upload is still necessary after filename-based routing
- Decide whether to add case persistence after the first internal test round
